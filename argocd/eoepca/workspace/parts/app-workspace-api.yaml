apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: workspace-api
  namespace: argocd
  labels:
    eoepca/app-name: workspace-api
spec:
  # Ref. https://argo-cd.readthedocs.io/en/stable/user-guide/application-specification/
  project: eoepca
  source:
    repoURL: https://eoepca.github.io/helm-charts-dev
    chart: rm-workspace-api
    helm:
      releaseName: workspace-api
      valuesObject:
        fullnameOverride: workspace-api
        image:
          tag: 2.0.0-rc.4
          pullPolicy: Always
        ingress:
          enabled: false        
        storageSecretName: "<principal>"
        prefixForName: "ws"
        endpoint: "https://minio.develop.eoepca.org"
        region: "eoepca-demo"
        forwardedAllowIps: "true"
        sessionMode: "on"
        useVcluster: "true"
        gunicornWorkers: "5"
        uiMode: "ui"
  destination:
    name: in-cluster
    namespace: workspace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
      - Validate=true
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - RespectIgnoreDifferences=true
      - ApplyOutOfSyncOnly=true
